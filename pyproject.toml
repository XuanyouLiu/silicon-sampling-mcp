[project]
name = "silicon-sampling-mcp"
version = "0.1.0"
description = "Structured Silicon Sampling (S3): a cognitively-grounded framework for silicon survey samples using Model Context Protocol"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]
